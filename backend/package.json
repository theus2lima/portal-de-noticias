{
  "name": "news-portal-backend",
  "version": "1.0.0",
  "description": "Backend API layer between Vercel frontend and Supabase — cache-first, rate-limited, horizontally scalable.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev":   "node --watch src/server.js",
    "start": "node src/server.js",
    "lint":  "node --check src/**/*.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors":       "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@supabase/supabase-js": "^2.45.0",
    "dotenv":              "^16.4.5",
    "fastify":             "^4.28.1",
    "ioredis":             "^5.4.1"
  },
  "devDependencies": {
    "pino-pretty": "^11.2.1"
  }
}
